name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

env:
  GO_PRIMARY: '1.26'

jobs:
  # ---------------------------------------------------------------------------
  # Lint
  # ---------------------------------------------------------------------------
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_PRIMARY }}
          cache: true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11

  # ---------------------------------------------------------------------------
  # Test (ubuntu-latest only, Go 1.25 + 1.26)
  # ---------------------------------------------------------------------------
  test:
    name: Test (Go ${{ matrix.go }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go: ['1.25', '1.26']

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: Download dependencies
        run: go mod tidy

      - name: Run tests
        run: make test

      - name: Check test coverage
        # Use pure shell arithmetic — no dependency on bc
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | awk '/^total:/{gsub(/%/,"",$3); print $3}')
          echo "Coverage: ${COVERAGE}%"
          # Multiply by 10 to work with integer comparison (avoids bc / awk -v)
          COVERAGE_INT=$(printf "%.0f" "$(echo "${COVERAGE} * 10" | awk '{printf "%f", $1 * $3}')")
          if [ "${COVERAGE_INT}" -lt 800 ]; then
            echo "Coverage is below 80% threshold (got ${COVERAGE}%)"
            exit 1
          fi

      - name: Upload coverage
        if: matrix.go == env.GO_PRIMARY
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.out
          flags: unittests
          name: codecov-umbrella

  # ---------------------------------------------------------------------------
  # Security scan (lint + test must pass first)
  # ---------------------------------------------------------------------------
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [lint, test]
    permissions:
      contents: read
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_PRIMARY }}
          cache: true

      - name: Run govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck -json ./... > govulncheck-results.json || true

      - name: Upload govulncheck results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: govulncheck-results
          path: govulncheck-results.json
          retention-days: 30

      - name: Comment PR with govulncheck results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            try {
              const results = fs.readFileSync('govulncheck-results.json', 'utf8');
              const findings = [];
              for (const line of results.split('\n')) {
                if (!line.trim()) continue;
                const entry = JSON.parse(line);
                if (entry.vulnerability) {
                  findings.push(`**${entry.vulnerability.id}**: ${entry.vulnerability.details}`);
                }
              }
              if (findings.length > 0) {
                const body = `## govulncheck Results\n\nFound ${findings.length} vulnerabilities:\n\n` +
                  findings.slice(0, 10).join('\n\n') +
                  (findings.length > 10 ? '\n\n... and more' : '');
                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body,
                });
              }
            } catch (e) {
              console.log('Could not parse govulncheck results:', e);
            }

      - name: Run Gosec Security Scanner
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec -exclude-generated -fmt json -out gosec-results.json ./... || true

      - name: Upload Gosec results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gosec-results
          path: gosec-results.json
          retention-days: 30

      - name: Comment PR with Gosec results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            try {
              const results = JSON.parse(fs.readFileSync('gosec-results.json', 'utf8'));
              const issues = results.Issues || [];
              if (issues.length > 0) {
                const body = `## Gosec Results\n\nFound ${issues.length} security issues:\n\n` +
                  issues.slice(0, 10).map(i =>
                    `**${i.severity}**: ${i.rule_id} - ${i.details} (${i.file}:${i.line})`
                  ).join('\n') +
                  (issues.length > 10 ? '\n\n... and more' : '');
                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body,
                });
              }
            } catch (e) {
              console.log('Could not parse Gosec results:', e);
            }

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Upload Trivy SARIF artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-results
          path: trivy-results.sarif
          retention-days: 30

  # ---------------------------------------------------------------------------
  # Docker smoke-test (verify the image builds and the health endpoint works)
  # ---------------------------------------------------------------------------
  docker-test:
    name: Docker Smoke Test
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t icap-mock:ci .

      - name: Run container and check health endpoint
        run: |
          docker run -d --name icap-ci \
            -p 1344:1344 -p 8080:8080 -p 9090:9090 \
            icap-mock:ci
          # The image ships wget; wait briefly for the server to start
          sleep 5
          docker exec icap-ci wget --no-verbose --tries=3 --retry-connrefused \
            --spider http://localhost:8080/health
          docker stop icap-ci
          docker rm icap-ci
